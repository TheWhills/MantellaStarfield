# Python (Mantella backend adapter)

starfield.py - the Mantella game adapter: asset pipeline (TTS wav -> wem -> animset -> ffxanim) and the per-line queue producer. NOTE: this is a REFERENCE COPY. The live file runs from C:\Users\elija\Mantella\src\games\starfield.py. This layer is expected to be replaced by the DwemerDistro serverside connectors during the CHIM port.
